<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.johade.quotem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Views_Activities.TimedGameActivity"></activity>
        <activity android:name=".Views_Activities.BaseGameActivity" />
        <activity
            android:name=".Views_Activities.EndlessGameActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Views_Activities.HighScoresActivity"
            android:label="@string/title_activity_high_scores"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views_Activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views_Activities.BasicGameActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>